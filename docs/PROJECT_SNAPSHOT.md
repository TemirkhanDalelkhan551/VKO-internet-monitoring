# Снимок проекта — 18 сентября 2026

Контрольная точка исходников, документации и воспроизводимых проверок. Снимок отправляется отдельным коммитом в main; его SHA указан в истории Git. Версия агента/установщика остаётся 1.0.0: изменение веб-панели не пересобирает и не обновляет установленную службу.

## Исходники и опубликованный сервер

| Компонент | Реализовано | Где доступно |
| --- | --- | --- |
| Windows-агент | 3–5 окон/сутки, пять метрик, ограничение нагрузки, heartbeat, очередь/повторы, DPAPI | Установлен на текущем ПК; второй физический ПК не проверен |
| EXE 1.0.0 | Установка/обновление службы, графическая активация и локальный статус | Локальный артефакт, без цифровой подписи |
| API/PostgreSQL | Приём/валидация, EventId, школы/линии/устройства, история/аналитика, инциденты | Render |
| Пользователи/роли/аудит | Пять ролей, области, сессии/отзыв, серверный actor, аудит до изменения | API Render; веб-администрирование ещё отсутствует |
| Обзор/карточки | Статусы/свежесть/агент, фильтры, устройства, история/графики, контакты/договоры | Render |
| Аналитика/CSV/XLSX | Календарный период, школа/компьютеры/столбцы, сводки школ, защита формул | Render |
| Карта | Проверяемые nullable координаты, цвета/метрики, общие фильтры | Render; реальные координаты ещё нужно заполнить |
| Справочники/коды | Школы, контакты, линии, реквизиты договора; одноразовый код с выбором привязки | Render |
| Веб-инциденты | Список/карточка, фильтры, создание, назначение, комментарии, статусы, история | Новый этап локально и в текущем снимке Git; ещё не развёрнут |

Публичный адрес: https://vko-internet-monitoring-api.onrender.com/ . Последний подтверждённый деплой: e1309181029904b961cfd1510b8d01fd520ebb49, Deploy succeeded / Live, Healthy / PostgreSQL Healthy. Проверены файлы новой версии и 401 для неавторизованной выдачи кодов. Новый Git-коммит автоматически не деплоится: render.yaml оставляет autoDeployTrigger off.

## Повторённые проверки текущего снимка

| Проверка | Результат |
| --- | --- |
| dotnet test Release | 210 тестов |
| node --test tests/web/*.test.mjs | 25 тестов |
| dotnet build Release | Без ошибок/предупреждений |
| Linux Dockerfile API | Успешная сборка |
| verify-api-integration.ps1 | 138 проверок |
| verify-map-integration.ps1 | 33 проверки |
| verify-report-integration.ps1 | 50 проверок |
| verify-directory-integration.ps1 | 32 проверки |
| verify-web-activation.ps1 | 17 проверок |
| verify-web-incidents.ps1 | 25 проверок |

Всего 295 интеграционных проверок, отдельно от 210 .NET и 25 JS. Каждый веб-сценарий повторён на свежей отдельной БД/порту, чтобы данные и rate limits предыдущего сценария не искажали результат. Браузерная проверка UI инцидентов: Administrator/Provider/School/District, создание/назначение/комментарий/статус, переход из школы, фильтры, ошибочные даты, черновик при обновлении, безопасный текст, выход и 390 px. Regional проверен интеграционно; полноценная приёмка второго ПК ещё не выполнена.

```powershell
dotnet test VkoInternetMonitoring.sln -c Release
dotnet build VkoInternetMonitoring.sln -c Release
node --test tests/web/*.test.mjs
docker build -f src/VkoMonitoring.Api/Dockerfile -t vko-api:snapshot .
pwsh -NoProfile -File scripts/verify-api-integration.ps1 -ApiPort 5084
```

Веб-сценарии: опубликовать API в отдельный artifacts/api-test, запустить start-web-test-fixtures.ps1 с этим каталогом и свободным портом, затем нужный verify-скрипт с тем же BaseUrl. Для каждой проверки использовать свежую fixture; остановить именно её PID после проверки. БД должны быть local vko_web_*/vko_verify_*, не рабочая vko_monitoring и не Render. Тестовые пароли: USER_ACCESS_TESTING.md. Приватные логи/базы/резервные копии и EXE остаются вне Git.

## Идентификация установщика

- Файл: artifacts/installer/VkoInternetMonitoringAgent-Setup-1.0.0-win-x64.exe.
- Размер: 37 253 782 байта.
- SHA-256: `4FAF4D2D1CB8A7264D7A53DBFA9E8B427508B143636D6399298DBE4DAE5DA955`.
- Это контрольная сумма существующего файла, не цифровая подпись и не подтверждение чистой установки. Артефакт не пересобирался в этом этапе.

## Границы готовности

Не завершены: публикация UI инцидентов; веб-экраны пользователей/аудита; расписание/пороги и удалённая конфигурация; обращение/AI с подтверждением; уведомления; аналитика районов/поставщиков; PDF; CI; резервные копии/хранение/удаление; централизованные логи; автоматические обновления; подпись EXE; шифрование очереди; нагрузка/24–72 часа/калибровка; физическая проверка нескольких линий, чистой установки/активации/перезагрузки/удаления на другом ПК и других Windows.

Бесплатная PostgreSQL Render по исходному развёртыванию истекает 18.10.2026; эту дату нужно перепроверить в панели перед эксплуатацией. Сервер может засыпать, первый запрос задерживаться. Утверждение «система полностью готова» этим снимком не делается. Дальнейшие приоритеты — PROJECT_CHECKLIST.md; показ — DEMO_CHECKLIST.md.
