# Снимок проекта — 19 сентября 2026

Контрольная точка исходников, документации и воспроизводимых проверок. Серверная реализация из `6955b32` опубликована на Render; исправление агента и установщика зафиксировано в `3c1da96`. Версия агента/установщика — 1.2.1.

Полная повторная проверка после полевого исправления 1.2.1 выполнена 19.09.2026. Добавлены CI, автоматическая проверка EXE, процедура тихого обновления, сбор метрик ресурсов и проверяемое локальное резервное копирование PostgreSQL. Формальная готовность по 183 пунктам проектного чек-листа составляет 85%; подробный расчёт и остаток приведены в [READINESS_ASSESSMENT.md](READINESS_ASSESSMENT.md).

## Исходники и опубликованный сервер

| Компонент | Реализовано | Где доступно |
| --- | --- | --- |
| Windows-агент | 3–5 окон/сутки, ручной тест, пять метрик, ограничение нагрузки, heartbeat, очередь/повторы, DPAPI | Установка и активация подтверждены на втором компьютере; расширенная полевая приёмка продолжается |
| EXE 1.2.1 | Обновление с восстановлением ранее запущенной службы, двухшаговая активация, локальный статус/диагностика и ручной тестовый замер | Локальный артефакт, без цифровой подписи |
| API/PostgreSQL | Приём/валидация, EventId, школы/линии/устройства, история/аналитика, инциденты | Render |
| Пользователи/роли/аудит | Пять ролей, области, сессии/отзыв, серверный actor, аудит до изменения | API Render; веб-администрирование ещё отсутствует |
| Обзор/карточки | Статусы/свежесть/агент, фильтры, устройства, история/графики, контакты/договоры | Render |
| Аналитика/CSV/XLSX | Календарный период, школа/компьютеры/столбцы, сводки школ, защита формул | Render |
| Карта | Проверяемые nullable координаты, цвета/метрики, общие фильтры | Render; реальные координаты ещё нужно заполнить |
| Справочники/коды | Школы, контакты, линии, реквизиты договора; одноразовый код с выбором привязки и предварительным просмотром без расходования | Render |
| Веб-инциденты | Список/карточка, фильтры, создание, назначение, комментарии, статусы, история | Render |

Публичный адрес: https://vko-internet-monitoring-api.onrender.com/ . Последний подтверждённый деплой: `6955b32c6cdf0ef4c5167c29ae6e969a7b5a4d63`, `Live`, readiness/PostgreSQL `Healthy`. Проверена доступность нового `/api/devices/activation-preview`. Новый Git-коммит автоматически не деплоится: `render.yaml` оставляет `autoDeployTrigger` выключенным.

## Повторённые проверки текущего снимка

| Проверка | Результат |
| --- | --- |
| dotnet test Release | 220 тестов |
| node --test tests/web/*.test.mjs | 25 тестов |
| dotnet build Release | Без ошибок/предупреждений |
| Linux Dockerfile API | Успешная сборка |
| verify-api-integration.ps1 | 138 проверок |
| verify-map-integration.ps1 | 33 проверки |
| verify-report-integration.ps1 | 50 проверок |
| verify-directory-integration.ps1 | 32 проверки |
| verify-web-activation.ps1 | 21 проверка |
| verify-web-incidents.ps1 | 25 проверок |

Всего 299 интеграционных проверок, отдельно от 220 .NET и 25 JS. Каждый веб-сценарий выполнен на свежей отдельной БД/порту, чтобы данные и rate limits предыдущего сценария не искажали результат. Браузерная проверка UI инцидентов: Administrator/Provider/School/District, создание/назначение/комментарий/статус, переход из школы, фильтры, ошибочные даты, черновик при обновлении, безопасный текст, выход и 390 px. Regional проверен интеграционно; установка и активация на втором компьютере подтверждены, расширенная полевая приёмка продолжается.

```powershell
dotnet test VkoInternetMonitoring.sln -c Release
dotnet build VkoInternetMonitoring.sln -c Release
node --test tests/web/*.test.mjs
docker build -f src/VkoMonitoring.Api/Dockerfile -t vko-api:snapshot .
pwsh -NoProfile -File scripts/verify-api-integration.ps1 -ApiPort 5084
```

Веб-сценарии: опубликовать API в отдельный artifacts/api-test, запустить start-web-test-fixtures.ps1 с этим каталогом и свободным портом, затем нужный verify-скрипт с тем же BaseUrl. Для каждой проверки использовать свежую fixture; остановить именно её PID после проверки. БД должны быть local vko_web_*/vko_verify_*, не рабочая vko_monitoring и не Render. Тестовые пароли: USER_ACCESS_TESTING.md. Приватные логи/базы/резервные копии и EXE остаются вне Git.

## Идентификация установщика

- Файл: artifacts/installer/VkoInternetMonitoringAgent-Setup-1.2.1-win-x64.exe.
- Размер: 37 264 431 байт.
- SHA-256: `3667EB6175E03CFAC2CEEDFA9549CD14E1BDC5E5C68C4ADF101E8E592CBAC0CD`.
- Установка и активация версии 1.1.0 на втором компьютере подтверждены; полевая проверка обновления 1.2.1 ещё предстоит.

## Границы готовности

Не завершены: веб-экраны пользователей/аудита; расписание/пороги и удалённая конфигурация; обращение/AI с подтверждением; уведомления; аналитика районов/поставщиков; PDF; CI; резервные копии/хранение/удаление; централизованные логи; автоматические обновления; подпись EXE; шифрование очереди; управляемое восстановление частично завершённой активации; реестр/отзыв кодов и устройств; нагрузка/24–72 часа/калибровка; физическая проверка нескольких линий, обновления 1.2.1, перезагрузки, офлайн-доставки и удаления на втором ПК и других Windows.

Бесплатная PostgreSQL Render по исходному развёртыванию истекает 18.10.2026; эту дату нужно перепроверить в панели перед эксплуатацией. Сервер может засыпать, первый запрос задерживаться. Утверждение «система полностью готова» этим снимком не делается. Дальнейшие приоритеты — PROJECT_CHECKLIST.md; показ — DEMO_CHECKLIST.md.
