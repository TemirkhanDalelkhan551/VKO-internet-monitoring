# Снимок проекта — 19 сентября 2026

Контрольная точка исходников, документации и воспроизводимых проверок. Серверная реализация из `f33c626` опубликована на Render. Версия агента/установщика — 1.3.0.

Полная повторная проверка после полевого исправления 1.2.1 выполнена 19.09.2026. Добавлены CI и Dependabot, аудит NuGet, автоматическая проверка EXE, процедура тихого обновления, безопасный диагностический ZIP, сбор метрик ресурсов, ротация резервных копий и проверка восстановления PostgreSQL. Работу второго компьютера пользователь принял без повторной полевой проверки. Формальная готовность по 183 пунктам проектного чек-листа составляет 91%; подробный расчёт и остаток приведены в [READINESS_ASSESSMENT.md](READINESS_ASSESSMENT.md).

## Исходники и опубликованный сервер

| Компонент | Реализовано | Где доступно |
| --- | --- | --- |
| Windows-агент | 3–5 окон/сутки, ручной тест, пять метрик, ограничение нагрузки, heartbeat, очередь/повторы, DPAPI | Работа второго компьютера принята пользователем |
| EXE 1.3.0 | Обновление с восстановлением ранее запущенной службы, двухшаговая активация, локальный статус/диагностика и ручной тестовый замер | Локальный артефакт, без цифровой подписи |
| API/PostgreSQL | Приём/валидация, EventId, школы/линии/устройства, история/аналитика, инциденты | Render |
| Пользователи/роли/аудит | Пять ролей, области, сессии/отзыв, серверный actor, аудит до изменения | Render |
| Обзор/карточки | Статусы/свежесть/агент, фильтры, устройства, история/графики, контакты/договоры | Render |
| Аналитика/CSV/XLSX | Календарный период, школа/компьютеры/столбцы, сводки школ, защита формул | Render |
| Карта | Проверяемые nullable координаты, цвета/метрики, общие фильтры | Render; реальные координаты ещё нужно заполнить |
| Справочники/коды | Школы, контакты, линии, договоры; выдача, статусы и отзыв кодов без хранения секрета в открытом виде | Render |
| Веб-операции | Пользователи, аудит, устройства, удалённые настройки, сводки, уведомления, обращение поставщику | Render |
| Веб-инциденты | Список/карточка, фильтры, создание, назначение, комментарии, статусы, история | Render |

Публичный адрес: https://vko-internet-monitoring-api.onrender.com/ . Последний подтверждённый деплой: `f33c6265f087e6abefdbc753af7685b233328c1d`, `Live`, readiness/PostgreSQL `Healthy`. Проверены новые статические модули, а административные настройки и реестр кодов без сессии возвращают 401; конфигурация агента без обязательной привязки отклоняется. GitHub Actions CI для этого SHA завершился успешно. Новый Git-коммит автоматически не деплоится: `render.yaml` оставляет `autoDeployTrigger` выключенным.

## Повторённые проверки текущего снимка

| Проверка | Результат |
| --- | --- |
| dotnet test Release | 222 теста |
| node --test tests/web/*.test.mjs | 28 тестов |
| dotnet build Release | Без ошибок/предупреждений |
| Linux Dockerfile API | Успешная сборка |
| verify-api-integration.ps1 | 142 проверки |
| verify-map-integration.ps1 | 33 проверки |
| verify-report-integration.ps1 | 50 проверок |
| verify-directory-integration.ps1 | 32 проверки |
| verify-web-activation.ps1 | 25 проверок |
| verify-web-incidents.ps1 | 25 проверок |
| check-vulnerable-packages.ps1 | 6 проектов, известных уязвимых NuGet-пакетов не найдено |
| backup-postgres.ps1 + verify-postgres-restore.ps1 | Ротация проверена; восстановлены и сверены 10 таблиц / 90 строк |
| collect-agent-diagnostics.ps1 | ZIP создан; токен и содержимое очереди отсутствуют, конфигурация очищена |

Всего 307 интеграционных проверок, отдельно от 222 .NET и 28 JS. Каждый веб-сценарий выполнен на свежей отдельной БД/порту, чтобы данные и rate limits предыдущего сценария не искажали результат. Браузерная проверка UI инцидентов: Administrator/Provider/School/District, создание/назначение/комментарий/статус, переход из школы, фильтры, ошибочные даты, черновик при обновлении, безопасный текст, выход и 390 px. Regional проверен интеграционно; работу второго компьютера пользователь принял 19.09.2026.

```powershell
dotnet test VkoInternetMonitoring.sln -c Release
dotnet build VkoInternetMonitoring.sln -c Release
node --test tests/web/*.test.mjs
docker build -f src/VkoMonitoring.Api/Dockerfile -t vko-api:snapshot .
pwsh -NoProfile -File scripts/verify-api-integration.ps1 -ApiPort 5084
```

Веб-сценарии: опубликовать API в отдельный artifacts/api-test, запустить start-web-test-fixtures.ps1 с этим каталогом и свободным портом, затем нужный verify-скрипт с тем же BaseUrl. Для каждой проверки использовать свежую fixture; остановить именно её PID после проверки. БД должны быть local vko_web_*/vko_verify_*, не рабочая vko_monitoring и не Render. Тестовые пароли: USER_ACCESS_TESTING.md. Приватные логи/базы/резервные копии и EXE остаются вне Git.

## Идентификация установщика

- Файл: artifacts/installer/VkoInternetMonitoringAgent-Setup-1.3.0-win-x64.exe.
- Размер: 37 264 442 байта.
- SHA-256: `E1053A4C51568AAAEDC6826654BA659CF6778EF6B831746F5653297B9CDFA807`.
- Установка и работа второго компьютера приняты пользователем; повторная полевая проверка не требуется.

## Границы готовности

Не завершены: AI-черновик отложен по решению пользователя; единый пошаговый мастер подключения; отдельный серверный PDF по утверждённому шаблону; внешнее зашифрованное хранение резервных копий; централизованные логи; автоматические обновления; подпись EXE; шифрование очереди; управляемое восстановление частично завершённой активации; реестр/отзыв кодов и устройств; нагрузка/24–72 часа/калибровка; физическая проверка нескольких линий и дополнительные версии Windows.

Бесплатная PostgreSQL Render по исходному развёртыванию истекает 18.10.2026; эту дату нужно перепроверить в панели перед эксплуатацией. Сервер может засыпать, первый запрос задерживаться. Утверждение «система полностью готова» этим снимком не делается. Дальнейшие приоритеты — PROJECT_CHECKLIST.md; показ — DEMO_CHECKLIST.md.
